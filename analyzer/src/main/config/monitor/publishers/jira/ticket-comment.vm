## Apache Velocity template
## Event
The application $application_type faced again the following event on $event.StartDate : 
#if( $event.NameExtraInfo )
  >>  $event.Name / $event.NameExtraInfo <<
#else
  >> $event.Name <<
#end

Recommendation is : $event.Message 

#if( $events_list.size() > 1 )
Event observed $events_list.size() times
#end
#if( $report_attachment_active )
See the attached JZR report for more details
#end

Event code is $event.Ref
It is classified as $event.Level $event.SubLevel
It affects the $event.Scope scope
#if( $events_list.size() > 1 )
Last event ended up on $last_event.EndDate
#else
Event ended up on $event.EndDate 
#end

Process details
JVM : @@java.vm.name@@ @@java.version@@
Jeyzer demo : %%jeyzer-demo%%

----------------------------------------------------
JIRA comment generated by Jeyzer Monitor